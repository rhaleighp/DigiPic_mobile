<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digipic"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigiPic"
        android:networkSecurityConfig="@xml/network_security_config"

        tools:targetApi="31">
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".LastActivity" />
        <activity android:name=".toLogin" />
        <activity android:name=".toSignup" />
        <activity android:name=".VerifyCodeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".VerifyActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".LessonAvailableActivity" />
        <activity android:name=".LessonCompletedActivity" />
        <activity android:name=".ModuleDetailActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".NewsFeedActivity" />




        <!-- Launcher / Startup Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DigiPic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
